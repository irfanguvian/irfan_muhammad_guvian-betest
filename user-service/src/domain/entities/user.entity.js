export class User {
    constructor({ accountNumber, identityNumber, userName, emailAddress }) {
        this.accountNumber = accountNumber;
        this.identityNumber = identityNumber;
        this.userName = userName;
        this.emailAddress = emailAddress;
    }
}